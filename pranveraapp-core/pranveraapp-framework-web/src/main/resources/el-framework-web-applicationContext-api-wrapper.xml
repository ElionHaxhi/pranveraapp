<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd">

    <!-- The following beans are for API Wrappers.  They must be prototype scoped..  Override for extensibility.  Unlike the other classes that are keyed by their interface name,
               these are keyed by their base implementation name. JAXB doesn't like interfaces too much, so we use a base wrapper class that can be extended and overwritten here. -->
    <bean id="com.pranveraapp.core.web.api.wrapper.AuthorWrapper" class="com.pranveraapp.core.web.api.wrapper.AuthorWrapper" scope="prototype"/>


</beans>
